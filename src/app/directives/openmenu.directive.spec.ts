import { OpenmenuDirective } from './openmenu.directive';

describe('OpenmenuDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenmenuDirective();
    expect(directive).toBeTruthy();
  });
});
